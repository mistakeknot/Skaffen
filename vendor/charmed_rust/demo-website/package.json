{
  "name": "charmed-wasm-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Interactive demo website for charmed_rust WASM bindings",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "build:wasm": "../scripts/build-wasm.sh web",
    "dev:with-wasm": "npm run build:wasm && npm run dev"
  },
  "dependencies": {
    "charmed-wasm": "file:../pkg/web"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
